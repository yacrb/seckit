# Linux Notes

Linux and WSL setup is now primarily handled by `bootstrap.sh` plus the category-focused submodules under `tools/`.

Use this directory for host-specific install notes, custom local additions, or legacy content that should not be folded into the curated category tree.
